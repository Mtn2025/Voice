# Test Environment Variables
# These values are ONLY for testing and should NEVER be used in production

# Database (Required by config validation)
POSTGRES_USER=test_user
POSTGRES_PASSWORD=test_password_DO_NOT_USE_IN_PROD
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=test_voice_db

# API Services (Optional - tests can mock these)
AZURE_SPEECH_KEY=test_azure_key
AZURE_SPEECH_REGION=eastus
GROQ_API_KEY=test_groq_key
GROQ_MODEL=llama-3.3-70b-versatile

# Twilio (Optional)
TWILIO_ACCOUNT_SID=test_twilio_sid
TWI LIO_AUTH_TOKEN=test_twilio_token

# Telnyx (Optional)
TELNYX_API_KEY=test_telnyx_key
TELNYX_PUBLIC_KEY=test_telnyx_public

# Admin (Optional)
ADMIN_API_KEY=test_admin_key

# Redis (Optional)
REDIS_URL=redis://localhost:6379/1

# Debug Mode
DEBUG=True
