# Environment variables and secrets - NEVER commit this file
# This file should only exist locally and in Coolify environment variables

# =============================================================================
# IMPORTANT SECURITY NOTES
# =============================================================================
# 1. This file (.env) is in .gitignore and should NEVER be committed to Git
# 2. In Coolify, configure these as Environment Variables in the dashboard
# 3. Never log, print, or expose these values in application output
# 4. Use .env.example as template for required variables
# 5. Rotate keys regularly (every 90 days recommended)

# =============================================================================
# AZURE COGNITIVE SERVICES (Speech-to-Text y Text-to-Speech)
# =============================================================================
# Obtener en: https://portal.azure.com → Cognitive Services → Speech
AZURE_SPEECH_KEY=your_azure_speech_key_here
AZURE_SPEECH_REGION=eastus

# =============================================================================
# GROQ API (Large Language Model)
# =============================================================================
# Obtener en: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile

# =============================================================================
# TWILIO (Opcional - para llamadas telefónicas)
# =============================================================================
# Obtener en: https://www.twilio.com/console
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here

# =============================================================================
# TELNYX (Opcional - alternativa a Twilio)
# =============================================================================
# Obtener en: https://telnyx.com/
TELNYX_API_KEY=your_telnyx_api_key_here
TELNYX_API_BASE=https://api.telnyx.com/v2

# =============================================================================
# DATABASE (PostgreSQL)
# =============================================================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_this_to_secure_password
POSTGRES_SERVER=db
POSTGRES_PORT=5432
POSTGRES_DB=voice_db

# =============================================================================
# AUTENTICACIÓN - ETAPA 1 (Dashboard Access)
# =============================================================================
# Generar con: python -c "import secrets; print(secrets.token_urlsafe(32))"
# IMPORTANTE: Guardar esta key de forma segura. Quien la tenga puede modificar
# toda la configuración del dashboard.
ADMIN_API_KEY=generate_with_command_above

# =============================================================================
# SECURITY BEST PRACTICES
# =============================================================================
#
# 1. NEVER commit this file to Git (already in .gitignore)
# 2. Use strong, random values for all keys and passwords
# 3. Rotate keys every 90 days
# 4. Use different keys for development/staging/production
# 5. Store keys in Coolify Environment Variables (not in this file in production)
# 6. Enable 2FA on all service accounts (Azure, Groq, Twilio, Telnyx)
# 7. Monitor API usage for anomalies
# 8. Use IP whitelisting where possible
# 9. Set up alerts for failed authentication attempts
# 10. Review access logs monthly
#
